---
phase: 01-内容事实契约与注册表
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/video-registry.ts
  - src/shared/video-registry.test.ts
autonomous: true
requirements:
  - REG-01
  - REG-02
user_setup: []
must_haves:
  truths:
    - 创作者可以从一个共享 registry 看到 composition、still、输出路径、模板类型和平台配置
    - registry 明确记录 safe area、subtitle mode、slot 能力与视频号预算，而不是只列出 ID
    - registry 的重复 ID 或缺失元数据会被自动测试阻止
  artifacts:
    - src/shared/video-registry.ts
    - src/shared/video-registry.test.ts
  key_links:
    - src/shared/video-registry.test.ts -> src/shared/video-registry.ts
    - src/shared/video-registry.ts -> src/Root.tsx
    - src/shared/video-registry.ts -> package.json
---

<objective>
建立统一模板注册表，把当前仓库的 composition、封面 still、输出路径、模板类型和平台约束从手写散点收敛到一个 typed metadata source。

Purpose: 先解决“到处找模板信息”的问题，为后续 Root / CLI / API 统一发现打好元数据底座。
Output: `VideoTemplateRegistry` schema、平台 profile 定义、当前视频模板 inventory，以及覆盖重复 / 缺字段风险的 registry 测试。
</objective>

<execution_context>
@/Users/qilin.guo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/qilin.guo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-内容事实契约与注册表/01-CONTEXT.md
@.planning/phases/01-内容事实契约与注册表/01-RESEARCH.md
@package.json
@src/Root.tsx
@src/compositions/index.ts
@src/shared/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义 registry entry schema 与平台 profile</name>
  <files>src/shared/video-registry.ts</files>
  <read_first>.planning/phases/01-内容事实契约与注册表/01-CONTEXT.md, .planning/phases/01-内容事实契约与注册表/01-RESEARCH.md, src/Root.tsx</read_first>
  <action>新增 `src/shared/video-registry.ts`，定义 `VideoTemplateRegistryEntrySchema`、`VideoPlatformProfileSchema`、`SafeAreaProfileSchema`、`SubtitleModeSchema` 与 `TemplateCapabilitySchema`。字段至少覆盖：`compositionId`、`stillId`、`templateType`、`aspectRatio`、`safeAreaProfile`、`subtitleMode`、`supports`（至少含 `hookSlot`、`coverSlot`、`proofSlot`）、`output.video`、`output.cover`、`platformProfiles.videoChannel.titleMaxChars`、`descriptionMaxChars`、`coverAspectRatio`、`defaultPackageItems`。命名上优先保持可读性，不要把平台约束散落进多个常量文件。</action>
  <verify>npx vitest run src/shared/video-registry.test.ts</verify>
  <acceptance_criteria>
    - `src/shared/video-registry.ts` contains `export const VideoTemplateRegistryEntrySchema`
    - `src/shared/video-registry.ts` contains `videoChannel`
    - `src/shared/video-registry.ts` contains `hookSlot`
    - `src/shared/video-registry.ts` contains `defaultPackageItems`
  </acceptance_criteria>
  <done>registry schema 能表达模板发现、封面 still、输出路径与平台预算，不再只是 composition 名单。</done>
</task>

<task type="auto">
  <name>Task 2: 盘点当前 composition 并落地 metadata registry</name>
  <files>src/shared/video-registry.ts</files>
  <read_first>src/Root.tsx, src/compositions/index.ts, package.json</read_first>
  <action>在同一文件中创建 `videoTemplateRegistry` 常量，覆盖当前 `Root.tsx` 已注册的短视频 composition，以及对应的 cover still（有则填入、没有则显式标记）。输出路径优先复用现有渲染命令和仓库约定，统一收敛到 `out/` 目录下的视频与封面文件名。为每个条目填入 `templateType`、`aspectRatio`、`safeAreaProfile`、`subtitleMode`、`supports`、`platformProfiles`，并提供 `listVideoTemplates()`、`getVideoTemplateByCompositionId()`、`getPlatformProfile()` 这类只读 helper。这里保持 metadata-only，不直接引入 React 组件引用，避免与 `Root.tsx` 形成循环依赖。</action>
  <verify>npx vitest run src/shared/video-registry.test.ts</verify>
  <acceptance_criteria>
    - `src/shared/video-registry.ts` contains `export const videoTemplateRegistry`
    - `src/shared/video-registry.ts` contains `listVideoTemplates`
    - `src/shared/video-registry.ts` contains `getVideoTemplateByCompositionId`
    - `src/shared/video-registry.ts` contains `safeAreaProfile`
  </acceptance_criteria>
  <done>当前仓库的模板 inventory 已收敛为一个共享 metadata source，后续 Root / API 可以直接消费。</done>
</task>

<task type="auto">
  <name>Task 3: 为 registry 完整性和重复风险补测试</name>
  <files>src/shared/video-registry.test.ts</files>
  <read_first>src/shared/video-registry.ts, .planning/REQUIREMENTS.md</read_first>
  <action>新增 `src/shared/video-registry.test.ts`，至少覆盖 4 类验证：1) 每条 registry entry 都能通过 schema；2) `compositionId` 不重复；3) 每条 entry 都有 `output.video`、`templateType`、`safeAreaProfile`、`subtitleMode`、`platformProfiles.videoChannel`；4) 读取 helper 能按 `compositionId` 查询并返回 still / output / platform metadata。测试不要依赖 Remotion studio，只验证共享元数据行为。</action>
  <verify>npx vitest run src/shared/video-registry.test.ts</verify>
  <acceptance_criteria>
    - `src/shared/video-registry.test.ts` contains `describe("videoTemplateRegistry"`
    - `src/shared/video-registry.test.ts` contains `compositionId`
    - `src/shared/video-registry.test.ts` contains `videoChannel`
  </acceptance_criteria>
  <done>registry 的结构完整性与 helper 行为有自动保护，后续再接 Root / API 时不需要重新猜元数据。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npx vitest run src/shared/video-registry.test.ts`
- [ ] 抽查至少 1 条 registry entry，确认 still / output / platform profile 都可从 helper 读到
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- registry 可单点回答“有哪些模板、对应什么 still、输出到哪里、适合哪个平台配置”
- Phase 1 后续不再需要从 `Root.tsx` 和 `package.json` 手工拼 metadata
- registry 保持 metadata-only，为 01-03 的 Root / API 绑定留下清晰接口
  </success_criteria>

<output>
After completion, create `.planning/phases/01-内容事实契约与注册表/01-02-SUMMARY.md`
</output>
