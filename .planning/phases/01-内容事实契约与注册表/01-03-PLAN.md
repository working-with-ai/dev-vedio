---
phase: 01-内容事实契约与注册表
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/compositions/catalog.ts
  - src/Root.tsx
  - src/shared/types.ts
  - src/shared/render-contract.ts
  - src/shared/render-contract.test.ts
  - src/server/services/renderer.ts
  - src/server/routes/render.ts
autonomous: true
requirements:
  - BRIEF-03
  - REG-01
  - REG-02
user_setup: []
must_haves:
  truths:
    - render request 可以携带经过校验的 `brief`
    - API discoverability 返回的是 registry-backed metadata，而不只是 composition ID 列表
    - Root 注册和 API 发现消费同一套 catalog / registry 关系，减少入口漂移
  artifacts:
    - src/compositions/catalog.ts
    - src/Root.tsx
    - src/shared/types.ts
    - src/shared/render-contract.ts
    - src/shared/render-contract.test.ts
    - src/server/services/renderer.ts
    - src/server/routes/render.ts
  key_links:
    - src/compositions/catalog.ts -> src/shared/video-registry.ts
    - src/shared/types.ts -> src/shared/video-brief.ts
    - src/shared/render-contract.ts -> src/shared/video-registry.ts
    - src/server/services/renderer.ts -> src/shared/render-contract.ts
    - src/Root.tsx -> src/compositions/catalog.ts
---

<objective>
把 brief 与 registry 真正接到 Root / CLI / API 的统一发现合同上：让 render request brief-aware，让 discoverability 返回完整 metadata，并让 Root 注册不再单独维护模板清单。

Purpose: 只有入口真正共享合同，Phase 1 才算完成；否则 brief 和 registry 仍只是孤立文档。
Output: 共享 render resolver、catalog adapter、brief-aware `RenderRequestSchema`，以及 Root / renderer / route 的统一发现接线。
</objective>

<execution_context>
@/Users/qilin.guo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/qilin.guo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-内容事实契约与注册表/01-CONTEXT.md
@.planning/phases/01-内容事实契约与注册表/01-RESEARCH.md
@src/Root.tsx
@src/compositions/index.ts
@src/shared/types.ts
@src/server/services/renderer.ts
@src/server/routes/render.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 catalog adapter 与共享 render resolver</name>
  <files>src/compositions/catalog.ts, src/shared/render-contract.ts</files>
  <read_first>src/Root.tsx, src/compositions/index.ts, src/server/services/renderer.ts</read_first>
  <action>新增 `src/compositions/catalog.ts`，让它把现有 composition / still 组件、schema、defaultProps 与 `videoTemplateRegistry` 的 metadata 以同一 ID 绑定起来。新增 `src/shared/render-contract.ts`，导出 `resolveRenderInput()`、`buildDiscoveryPayload()`、`assertRegistryCoverage()` 这类纯函数：前者把 `brief` 与 `inputProps` 组合成 render seed，后者把 registry/catalg 信息整理成 API discoverability 响应，后者则在启动时阻止 catalog 与 registry 漂移。这里不要把渲染副作用写进 resolver，保持为可测试的纯合同层。</action>
  <verify>npx vitest run src/shared/render-contract.test.ts</verify>
  <acceptance_criteria>
    - `src/compositions/catalog.ts` contains `videoCompositionCatalog`
    - `src/compositions/catalog.ts` contains `videoTemplateRegistry`
    - `src/shared/render-contract.ts` contains `resolveRenderInput`
    - `src/shared/render-contract.ts` contains `buildDiscoveryPayload`
  </acceptance_criteria>
  <done>Root 与 API 有了共同消费的 catalog / registry 适配层，不必再各自维护模板来源。</done>
</task>

<task type="auto">
  <name>Task 2: 让 render route 与 renderer 变成 brief-aware discoverability 入口</name>
  <files>src/shared/types.ts, src/server/services/renderer.ts, src/server/routes/render.ts</files>
  <read_first>src/shared/types.ts, src/server/services/renderer.ts, src/server/routes/render.ts</read_first>
  <action>更新 `src/shared/types.ts` 中的 `RenderRequestSchema`，显式加入 `brief: VideoBriefSchema.optional()`，并为 discoverability response 补充 still / output / template / platform metadata 类型。随后在 `src/server/services/renderer.ts` 中改用 `resolveRenderInput()` 与 `buildDiscoveryPayload()`，让 `/api/render/compositions` 返回 registry-backed 列表，而不是仅依赖 `getCompositions()` 的原始结果。`src/server/routes/render.ts` 继续保留安全校验和 400/500 返回约定，但请求体和响应 shape 必须以共享 schema 为准。</action>
  <verify>npm run typecheck && npx vitest run src/shared/render-contract.test.ts</verify>
  <acceptance_criteria>
    - `src/shared/types.ts` contains `brief: VideoBriefSchema.optional()`
    - `src/server/services/renderer.ts` contains `resolveRenderInput`
    - `src/server/services/renderer.ts` contains `buildDiscoveryPayload`
    - `src/server/routes/render.ts` contains `RenderRequestSchema`
  </acceptance_criteria>
  <done>API 层可以接收结构化 brief，并返回包含 still / output / platform metadata 的 discoverability 数据。</done>
</task>

<task type="auto">
  <name>Task 3: 用 catalog 驱动 Root 注册并补统一合同测试</name>
  <files>src/Root.tsx, src/shared/render-contract.test.ts</files>
  <read_first>src/Root.tsx, src/compositions/catalog.ts, src/shared/render-contract.ts</read_first>
  <action>重构 `src/Root.tsx`，把现有手写的 composition / still 注册收敛到 `videoCompositionCatalog` 和可能的 `videoStillCatalog` 映射上，至少让“哪些模板存在”这件事不再在 Root 里重复一份。新增或补充 `src/shared/render-contract.test.ts`，验证 3 件事：1) `resolveRenderInput()` 会把 `brief` 的发布字段并入 render props seed；2) `buildDiscoveryPayload()` 暴露 still / output / templateType / platformProfiles；3) catalog 与 registry ID 集合一致时通过，不一致时失败。不要在 Phase 1 引入端到端渲染测试；这里的目标是锁定统一合同，而不是验证成片视觉。</action>
  <verify>npm run typecheck && npx vitest run src/shared/render-contract.test.ts</verify>
  <acceptance_criteria>
    - `src/Root.tsx` contains `videoCompositionCatalog.map`
    - `src/shared/render-contract.test.ts` contains `buildDiscoveryPayload`
    - `src/shared/render-contract.test.ts` contains `resolveRenderInput`
    - `src/shared/render-contract.test.ts` contains `assertRegistryCoverage`
  </acceptance_criteria>
  <done>Root、CLI 和 API 的模板发现都由统一 catalog / registry 关系驱动，Phase 1 的单一真相真正生效。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npx vitest run src/shared/render-contract.test.ts`
- [ ] 启动 API 后确认 `/api/render/compositions` 返回 still / output / template / platform metadata
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- brief、registry 与 discoverability 真正接到同一条 Root / API / CLI 合同链路上
- 新增 composition 时不再需要同时维护多处模板列表
- Phase 2 可以直接在 shared contract 上继续叠加 media manifest 与 QA 门禁
  </success_criteria>

<output>
After completion, create `.planning/phases/01-内容事实契约与注册表/01-03-SUMMARY.md`
</output>
