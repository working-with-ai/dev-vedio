---
phase: 01-内容事实契约与注册表
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - src/shared/video-brief.ts
  - src/shared/video-brief.test.ts
autonomous: true
requirements:
  - BRIEF-01
  - BRIEF-02
  - BRIEF-03
user_setup: []
must_haves:
  truths:
    - 缺少核心事实或来源链接的 brief 会被明确拒绝
    - 同一份 brief 能导出脚本、封面、标题和描述共用的发布 seed
    - 共享合同层不再只靠 typecheck，已有最小可运行的 Vitest 校验
  artifacts:
    - package.json
    - vitest.config.ts
    - src/shared/video-brief.ts
    - src/shared/video-brief.test.ts
  key_links:
    - src/shared/video-brief.test.ts -> src/shared/video-brief.ts
    - src/shared/video-brief.ts -> src/shared/types.ts
---

<objective>
建立 Phase 1 的 `VideoBrief` 真相源：补齐最小 Vitest 基础设施，定义可验证的 brief schema，并提供把 brief 派生成脚本 / 封面 / 标题 / 描述 seed 的共享 helper。

Purpose: 先把“什么事实必须存在、缺什么必须报错、哪些包装字段必须与旁白共源”固定下来，避免后续模板和发布层继续人工拼接。
Output: `vitest` 基础配置、`VideoBriefSchema`、brief derivation helper，以及覆盖必填字段与拒绝路径的合同测试。
</objective>

<execution_context>
@/Users/qilin.guo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/qilin.guo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-内容事实契约与注册表/01-CONTEXT.md
@.planning/phases/01-内容事实契约与注册表/01-RESEARCH.md
@docs/ai-harness-engineer-voiceover-script.md
@docs/ai-harness-engineer-copy.md
@package.json
@src/shared/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 安装合同测试基础设施并固定命令入口</name>
  <files>package.json, vitest.config.ts</files>
  <read_first>package.json, .planning/phases/01-内容事实契约与注册表/01-RESEARCH.md</read_first>
  <action>在 `package.json` 中增加 `vitest` 开发依赖，并补两个明确命令：`"test": "vitest run"` 与 `"test:contracts": "vitest run src/shared/**/*.test.ts src/server/**/*.test.ts"`。新增 `vitest.config.ts`，使用 `environment: "node"`、`globals: true`、`include: ["src/**/*.test.ts"]`，不启用 watch mode，不引入浏览器或 UI 测试插件。这里的目标是为 Phase 1 的共享合同层提供最小可运行基础，而不是一次性上完整测试栈。</action>
  <verify>npm install && npx vitest run --passWithNoTests</verify>
  <acceptance_criteria>
    - `package.json` contains `"test": "vitest run"`
    - `package.json` contains `"test:contracts": "vitest run src/shared/**/*.test.ts src/server/**/*.test.ts"`
    - `package.json` contains `"vitest"`
    - `vitest.config.ts` contains `environment: "node"`
  </acceptance_criteria>
  <done>仓库具备稳定的非 watch 合同测试入口，后续计划无需再为测试基础设施重复铺路。</done>
</task>

<task type="auto">
  <name>Task 2: 定义 VideoBriefSchema 与发布 seed 派生 helper</name>
  <files>src/shared/video-brief.ts</files>
  <read_first>.planning/phases/01-内容事实契约与注册表/01-CONTEXT.md, docs/ai-harness-engineer-voiceover-script.md, docs/ai-harness-engineer-copy.md, src/shared/types.ts</read_first>
  <action>新增 `src/shared/video-brief.ts`，用 Zod 定义 `VideoBriefSchema`、`VideoBriefInput`、`VideoBrief` 和 `derivePublishingSeed()`。必填字段至少覆盖：`topic`、`coreConclusion`、`coreFacts`、`sourceLinks`、`hookLine`、`hookProof`、`coreContrast`、`coverTitle`、`coverSubtitle`、`shortTitle`、`shortDescription`、`cta`、`visualTheme`。同时把节奏字段也显式纳入合同：`targetDurationSec`、`visualModules`、`sceneResetPlan`、`subtitleDensityMode`。实现约束时要明确拒绝空数组或空字符串；`sourceLinks` 必须是 URL；`visualModules` 要至少表达 3 类模块轮换能力，可以用 `.min(3)` 或默认三项配置，但不要让该需求只存在于注释里。`derivePublishingSeed()` 必须返回脚本 / 封面 / 标题 / 描述共用的稳定字段集合，而不是仅原样透传。</action>
  <verify>npx vitest run src/shared/video-brief.test.ts</verify>
  <acceptance_criteria>
    - `src/shared/video-brief.ts` contains `export const VideoBriefSchema`
    - `src/shared/video-brief.ts` contains `hookProof`
    - `src/shared/video-brief.ts` contains `sourceLinks`
    - `src/shared/video-brief.ts` contains `derivePublishingSeed`
  </acceptance_criteria>
  <done>共享 brief 合同可以表达前三秒 promise / proof、封面包装和节奏提示，并能为下游产物提供统一 seed。</done>
</task>

<task type="auto">
  <name>Task 3: 用合同测试锁定拒绝路径与派生行为</name>
  <files>src/shared/video-brief.test.ts</files>
  <read_first>src/shared/video-brief.ts, .planning/REQUIREMENTS.md</read_first>
  <action>新增 `src/shared/video-brief.test.ts`，至少覆盖 4 类行为：1) 合法 brief 可通过；2) 缺少 `coreFacts` 时被拒绝；3) 缺少 `sourceLinks` 或 URL 非法时被拒绝；4) `derivePublishingSeed()` 会输出与 `coverTitle`、`shortTitle`、`shortDescription`、`hookLine`、`hookProof` 一致的共享结果。测试命名要直接对应 `BRIEF-01/02/03` 的行为，不写脆弱快照，不依赖渲染环境。</action>
  <verify>npx vitest run src/shared/video-brief.test.ts</verify>
  <acceptance_criteria>
    - `src/shared/video-brief.test.ts` contains `describe("VideoBriefSchema"`
    - `src/shared/video-brief.test.ts` contains `rejects`
    - `src/shared/video-brief.test.ts` contains `derivePublishingSeed`
  </acceptance_criteria>
  <done>关键拒绝路径和 seed 派生逻辑有可运行测试保护，后续计划改字段时能立即收到反馈。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npx vitest run src/shared/video-brief.test.ts`
- [ ] 用一个 reference brief 验证 `derivePublishingSeed()` 同时包含 hook、封面、标题和描述字段
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `VideoBriefSchema` 覆盖 roadmap 要求的事实、来源、CTA 与视觉主题字段
- 缺失核心事实或来源的 brief 会在测试中被明确拒绝
- Phase 1 后续计划可以直接复用 `VideoBrief`，不再重新定义内容合同
  </success_criteria>

<output>
After completion, create `.planning/phases/01-内容事实契约与注册表/01-01-SUMMARY.md`
</output>
